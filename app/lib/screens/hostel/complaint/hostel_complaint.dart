import 'package:flutter/material.dart';

class HostelComplaint extends StatelessWidget {
  const HostelComplaint({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
